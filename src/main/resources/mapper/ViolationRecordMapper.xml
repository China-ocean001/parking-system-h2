<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.parking.mapper.ViolationRecordMapper">
    <insert id="insert" parameterType="com.parking.entity.ViolationRecord">
        INSERT INTO violation_record (plate_number, violation_type, description) 
        VALUES (#{plateNumber}, #{violationType}, #{description})
    </insert>
    <select id="findAll" resultType="com.parking.entity.ViolationRecord">
        SELECT * FROM violation_record ORDER BY record_time DESC
    </select>
</mapper>
